/** @type {import('next').NextConfig} */
// const nextConfig = {}
// next.config.js
const nextConfig = {
  distDir: 'build', // Use 'build' as the custom build directory
};

module.exports = nextConfig
